<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <LinearLayout
            android:layout_width="match_parent"
            android:orientation="vertical"
            android:gravity="center_horizontal"
            android:layout_height="wrap_content">

            <TextView
                android:id="@+id/btnRippleScreenWidth"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:gravity="center_horizontal"
                android:foreground="@drawable/ripple_effect"
                android:paddingHorizontal="32dp"
                android:paddingVertical="16dp"
                android:text="Ripple"/>

            <TextView
                android:id="@+id/btnRippleR8"
                android:layout_width="200dp"
                android:gravity="center_horizontal"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:background="@drawable/shape_gray_r8"
                android:foreground="@drawable/ripple_effect_round"
                android:paddingHorizontal="32dp"
                android:paddingVertical="16dp"
                android:text="Ripple"/>


            <TextView
                android:id="@+id/btnRipple"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:background="@drawable/shape_gray_r45"
                android:foreground="@drawable/ripple_effect_circle"
                android:paddingHorizontal="32dp"
                android:paddingVertical="16dp"
                android:text="Ripple"/>

            <ImageView
                android:id="@+id/ivRipple"
                android:layout_width="44dp"
                android:layout_height="44dp"
                android:foreground="@drawable/ripple_effect_circle"
                android:layout_marginTop="16dp"
                android:scaleType="centerInside"
                android:src="@android:drawable/ic_input_add"/>

        </LinearLayout>
    </androidx.core.widget.NestedScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>